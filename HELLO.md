# Hello from githog

githog is a config-driven provisioner that spins up isolated git worktrees and runs an agent loop in each one — re-invoking a coding agent with a clean context every iteration until your issue is done.
