# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# homestead worktree-local spawn marker (written inside each worktree, like .env)
.homestead-agent.json

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# vendored effect source (effect-ts skill) — cloned by `bun run prepare`
.repos/effect
/TASKS.md

# local herdr/agent worktrees (never commit)
.claude/worktrees/
